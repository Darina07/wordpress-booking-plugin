<?php
/* Template Name: Calendar */

//use calendar\Frontend\BookedHoursHandler;


/*$booked_hours_handler = new BookedHoursHandler();

// Function to enqueue scripts
function my_enqueue() {
    // Register the script
    wp_register_script('ajax-script', plugin_dir_url(__FILE__) . '../assets/js/script.js', array());

    // Localize the script after registering it
    wp_localize_script('ajax-script', 'my_ajax_object', array('ajax_url' => admin_url('admin-ajax.php')));

    // Enqueue the script
    wp_enqueue_script('ajax-script');
}
//add_action('wp_enqueue_scripts', 'my_enqueue');*/

